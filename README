# 权限管理系统

[![Go Version](https://img.shields.io/badge/Go-1.23.6-00ADD8?style=flat&logo=go)](https://golang.org/)
[![Gin](https://img.shields.io/badge/Gin-1.9.1-00ADD8?style=flat&logo=gin)](https://gin-gonic.com/)
[![MySQL](https://img.shields.io/badge/MySQL-5.7-4479A1?style=flat&logo=mysql)](https://www.mysql.com/)
[![Redis](https://img.shields.io/badge/Redis-7.0-DC382D?style=flat&logo=redis)](https://redis.io/)
[![React](https://img.shields.io/badge/React-18-61DAFB?style=flat&logo=react)](https://react.dev/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

基于 Go + React 的企业级 RBAC 权限管理系统，采用前后端分离架构。提供完整的用户认证、角色权限管理、注册审批、菜单管理等核心功能，适合作为运维 CMDB 系统的权限治理底座。

## 功能特性

### 认证与登录

- 用户名密码登录（图形验证码防暴力破解）
- 邮箱验证码登录（Redis 存储，支持冷却时间）
- JWT Token 认证（可配置有效期）
- 登录状态管理与安全退出
- 注册申请机制（提交后需管理员审核通过才可使用）

### 用户管理

- 用户增删改查（分页、关键词搜索）
- 用户详情查看（弹窗展示完整信息）
- 用户状态管理（启用 / 禁用）
- 用户角色分配（多角色绑定）
- 个人信息编辑（昵称修改）

### 角色管理

- 角色增删改查（分页、关键词搜索）
- 角色详情查看（弹窗展示完整信息）
- 角色状态管理（启用 / 禁用）

### 权限（API）管理

- 权限增删改查（分页、关键词搜索）
- 权限详情查看（弹窗展示完整信息）
- 资源路径 × 动作矩阵管理（GET / POST / PUT / DELETE）

### 授权策略管理

- Casbin RBAC 策略模型（p, p2, g 三层策略）
- 角色-权限授权（授予 / 回收）
- 策略列表查询与筛选

### 注册审核

- 用户注册申请列表（支持状态筛选：待审核 / 已通过 / 已拒绝）
- 管理员审核操作（通过后自动创建账号 / 拒绝并填写原因）
- 防重复提交校验（同用户名/邮箱的待审核记录互斥）

### 菜单管理

- 树形菜单结构（支持多级嵌套，parent_id 自引用）
- 菜单树接口（前端 Table / Tree 双模式展示）
- 新增根菜单 / 新增子菜单
- 编辑菜单（名称、路径、图标、排序、隐藏、组件等）
- 删除菜单（有子菜单时阻止删除）

### 系统监控

- 健康检查接口（服务状态、版本号、运行时长）
- 仪表盘总览（用户数、角色数、权限数、授权数统计）
- 最新授权编排列表

### 日志系统

- 分级日志输出（info.log / error.log / sql.log 三文件分离）
- 输出位置可控（console / file / both）
- SQL 日志桥接（GORM 自定义 Logger → slog 文件日志）
- 调用位置追踪（文件名:行号 + 函数名）

### 邮件服务

- SMTP 邮件发送（支持 TLS）
- 验证码邮件模板
- 登录验证码 & 注册验证码双场景

### API 文档

- Swagger UI 在线文档（`/swagger/index.html`）
- 接口自动注解生成
- 在线调试测试

## 技术栈

### 后端

| 技术 | 说明 | 版本 |
|------|------|------|
| [Go](https://golang.org/) | 编程语言 | 1.23+ |
| [Gin](https://gin-gonic.com/) | Web 框架 | 1.9+ |
| [GORM](https://gorm.io/) | ORM 框架 | 1.25+ |
| [MySQL](https://www.mysql.com/) | 关系数据库 | 5.7+ |
| [Redis](https://redis.io/) | 缓存 / 验证码存储 | 6.0+ |
| [Casbin](https://casbin.org/) | RBAC 权限引擎 | 2.77+ |
| [JWT](https://github.com/golang-jwt/jwt) | Token 认证 | 5.2+ |
| [Cobra](https://github.com/spf13/cobra) | CLI 命令框架 | 1.8+ |
| [Viper](https://github.com/spf13/viper) | 配置管理 | 1.18+ |
| [slog](https://pkg.go.dev/log/slog) | 标准日志库 | Go 1.21+ |
| [Swagger](https://github.com/swaggo/swag) | API 文档 | 1.16+ |

### 前端

| 技术 | 说明 | 版本 |
|------|------|------|
| [React](https://react.dev/) | UI 框架 | 18+ |
| [TypeScript](https://www.typescriptlang.org/) | 类型系统 | 5.3+ |
| [Ant Design](https://ant.design/) | 组件库 | 5.x |
| [Vite](https://vitejs.dev/) | 构建工具 | 5.x |
| [React Router](https://reactrouter.com/) | 路由 | 6.x |
| [Axios](https://axios-http.com/) | HTTP 客户端 | 1.x |

## 项目结构

```
go-permission-system/
├── cmd/                          # CLI 入口
│   ├── root.go                   # 根命令定义
│   ├── server.go                 # 启动 HTTP 服务
│   ├── migrate.go                # 数据库表迁移（AutoMigrate）
│   └── seed.go                   # 种子数据初始化
├── configs/
│   ├── config.yaml               # 主配置文件
│   └── casbin_model.conf         # Casbin RBAC 模型
├── internal/
│   ├── bootstrap/                # 应用构建器（依赖注入）
│   │   └── app.go                # Builder 模式组装 App
│   ├── config/                   # 配置结构体 & 加载
│   │   └── config.go             # Viper 配置映射
│   ├── handler/                  # HTTP 处理器
│   │   ├── auth_handler.go       # 登录/注册/验证码
│   │   ├── user_handler.go       # 用户 CRUD
│   │   ├── role_handler.go       # 角色 CRUD
│   │   ├── permission_handler.go # 权限 CRUD
│   │   ├── policy_handler.go     # 策略授权
│   │   ├── menu_handler.go       # 菜单 CRUD
│   │   ├── registration_handler.go # 注册审核
│   │   └── system_handler.go     # 健康检查
│   ├── middleware/                # 中间件
│   │   ├── auth.go               # JWT 认证中间件
│   │   ├── authorize.go          # Casbin 权限鉴权
│   │   ├── recovery.go           # 异常恢复
│   │   └── logger.go             # 请求日志
│   ├── model/                    # 数据模型
│   │   ├── user.go               # User + UserRole
│   │   ├── role.go               # Role
│   │   ├── permission.go         # Permission
│   │   ├── menu.go               # Menu（树形结构）
│   │   └── registration_request.go # RegistrationRequest
│   ├── repository/               # 数据访问层
│   │   ├── user_repository.go
│   │   ├── role_repository.go
│   │   ├── permission_repository.go
│   │   ├── policy_repository.go
│   │   ├── menu_repository.go    # 含 buildMenuTree
│   │   └── registration_request_repository.go
│   ├── service/                  # 业务逻辑层
│   │   ├── auth_service.go       # 认证逻辑
│   │   ├── user_service.go       # 用户业务
│   │   ├── role_service.go       # 角色业务
│   │   ├── permission_service.go # 权限业务
│   │   ├── policy_service.go     # 策略业务
│   │   ├── menu_service.go       # 菜单业务
│   │   └── registration_service.go # 注册审核流程
│   ├── pkg/
│   │   ├── logger/               # 日志系统
│   │   │   ├── logger.go         # Logger（Info/Error/SQL 三日志）
│   │   │   └── gorm_logger.go    # GORM → slog 桥接
│   │   ├── mailer/               # SMTP 邮件发送
│   │   ├── password/             # bcrypt 密码哈希
│   │   └── response/             # 统一响应格式
│   ├── store/                    # Redis 键名管理
│   └── router/                   # 路由注册
│       └── router.go             # 全部路由 & 中间件挂载
├── web/                          # 前端（React + Vite）
│   ├── src/
│   │   ├── pages/                # 页面组件
│   │   │   ├── dashboard-page.tsx    # 仪表盘（含系统状态）
│   │   │   ├── users-page.tsx        # 用户管理（含详情弹窗）
│   │   │   ├── roles-page.tsx        # 角色管理（含详情弹窗）
│   │   │   ├── permissions-page.tsx  # 权限管理（含详情弹窗）
│   │   │   ├── policies-page.tsx     # 授权编排
│   │   │   ├── registrations-page.tsx # 注册审核
│   │   │   ├── menus-page.tsx        # 菜单管理
│   │   │   └── login-page.tsx        # 登录页（三 Tab）
│   │   ├── services/            # API 服务层
│   │   ├── types/               # TypeScript 类型
│   │   ├── contexts/            # React Context
│   │   ├── components/          # 公共组件
│   │   ├── layouts/             # 布局组件
│   │   ├── constants/           # 品牌常量
│   │   └── app.tsx              # 路由配置
│   └── package.json
├── logs/                         # 运行时日志目录
├── go.mod / go.sum
└── README
```

## 快速开始

### 环境要求

| 依赖 | 最低版本 |
|------|---------|
| Go | >= 1.23 |
| MySQL | >= 5.7 |
| Redis | >= 6.0 |
| Node.js | >= 18 |

### 1. 克隆 & 安装依赖

```bash
git clone https://github.com/yourusername/go-permission-system.git
cd go-permission-system
go mod download
cd web && npm install
```

### 2. 修改配置

编辑 `configs/config.yaml`：

```yaml
app:
  name: permission-system
  env: dev
  port: 8080

mysql:
  host: 127.0.0.1
  port: 3306
  user: root
  password: "your_password"
  db_name: permission_system

redis:
  addr: 127.0.0.1:6379
  password: ""
  db: 0

mail:
  host: smtp.example.com
  port: 465
  username: your_email@example.com
  password: your_email_password
  from_email: your_email@example.com
  use_tls: true

auth:
  jwt_secret: change-me-in-production
  access_token_ttl_minutes: 120
  email_code_ttl_seconds: 600
  email_code_cooldown_seconds: 60

log:
  level: info
  format: text
  output: both              # console | file | both
  file_path: ./logs
```

### 3. 数据库迁移

```bash
go run . migrate
```

### 4. 初始化种子数据

```bash
go run . seed
```

默认创建：
- 管理员账号 `admin` / `Admin@123`
- Super Admin 角色（拥有全部权限）
- 26 条 API 权限数据
- 8 条默认菜单（资产总览 + 系统管理下 6 个子菜单）

### 5. 启动服务

```bash
# 后端（终端 1）
go run . server

# 前端开发服务器（终端 2）
cd web && npm run dev
```

访问地址：
- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:8080/api/v1`
- Swagger 文档：`http://localhost:8080/swagger/index.html`

## API 接口总览

### 系统接口（无需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/health` | 服务健康检查 |
| POST | `/api/v1/auth/login` | 用户名密码登录 |
| POST | `/api/v1/auth/email-login` | 邮箱验证码登录 |
| POST | `/api/v1/auth/logout` | 退出登录 |
| POST | `/api/v1/auth/register` | 提交注册申请 |
| POST | `/api/v1/auth/verification-code` | 发送邮箱验证码 |
| POST | `/api/v1/auth/password-login-code` | 发送密码登录图形验证码 |

### 业务接口（需 JWT + 权限）

#### 用户管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/users` | 用户列表（分页） |
| POST | `/api/v1/users` | 创建用户 |
| GET | `/api/v1/users/:id` | 用户详情 |
| PUT | `/api/v1/users/:id` | 更新用户 |
| DELETE | `/api/v1/users/:id` | 删除用户 |
| PUT | `/api/v1/users/:id/roles` | 分配角色 |

#### 角色管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/roles` | 角色列表（分页） |
| POST | `/api/v1/roles` | 创建角色 |
| GET | `/api/v1/roles/:id` | 角色详情 |
| PUT | `/api/v1/roles/:id` | 更新角色 |
| DELETE | `/api/v1/roles/:id` | 删除角色 |

#### 权限（API）管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/permissions` | 权限列表（分页） |
| POST | `/api/v1/permissions` | 创建权限 |
| GET | `/api/v1/permissions/:id` | 权限详情 |
| PUT | `/api/v1/permissions/:id` | 更新权限 |
| DELETE | `/api/v1/permissions/:id` | 删除权限 |

#### 授权策略

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/policies` | 策略列表 |
| POST | `/api/v1/policies` | 授予角色权限 |
| DELETE | `/api/v1/policies` | 回收角色权限 |

#### 注册审核

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/registrations` | 申请列表（支持状态/关键词筛选） |
| POST | `/api/v1/registrations/:id/review` | 审核操作（通过/拒绝） |

#### 菜单管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/menus/tree` | 菜单树（扁平→树形转换） |
| POST | `/api/v1/menus` | 创建菜单 |
| PUT | `/api/v1/menus/:id` | 更新菜单 |
| DELETE | `/api/v1/menus/:id` | 删除菜单（有子菜单时拦截） |

## 架构设计

### 分层架构

```
Handler（HTTP 层）
    ↓
Service（业务逻辑层）
    ↓
Repository（数据访问层）
    ↓
Model（数据模型） ← GORM AutoMigrate
```

### 认证流程

```
密码登录: 请求验证码 → 图形验证 → 校验账密 → 签发 JWT
邮箱登录: 发送邮箱验证码 → 输入验证码 → 签发 JWT
注册流程: 发送邮箱验证码 → 填写信息+验证码 → 提交申请(Pending) → 管理员审核(Approved/Rejected) → 通过则自动建号
```

### 权限控制流程

```
请求 → JWT 中间件（解析 Token → 注入用户身份）
    → Authorize 中间件（Casbin Enforcer.Enforce 判断）
    → Handler 执行业务逻辑
```

### 日志架构

```
应用日志 → slog.Logger → info.log / error.log
SQL 日志  → GORM 自定义 Logger → sql.log
输出目标  → config.output: console / file / both
```

## 开发指南

```bash
# 后端编译
go build ./...

# 后端代码格式化 & 检查
go fmt ./...
go vet ./...

# 前端开发
cd web && npm run dev

# 前端生产构建
cd web && npm run build

# 数据库重新迁移
go run . migrate

# 重新初始化种子数据
go run . seed
```

## 许可证

本项目采用 [MIT](LICENSE) 许可证开源。
